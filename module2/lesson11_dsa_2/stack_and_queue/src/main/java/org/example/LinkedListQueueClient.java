package org.example;

import java.util.List;
import java.util.Stack;
import java.util.TreeMap;

/**
 * Lớp `LinkedListQueueClient` dùng để kiểm tra hoạt động của hàng đợi (queue)
 * được triển khai bằng danh sách liên kết (linked list) thông qua lớp `MyLinkedListQueue`.
 *
 * Các thao tác được kiểm tra bao gồm:
 * - Thêm phần tử vào hàng đợi (enqueue).
 * - Lấy và xóa phần tử khỏi hàng đợi (dequeue).
 * - Xử lý khi hàng đợi rỗng.
 */
public class LinkedListQueueClient {
    public static void main(String[] args) {
//        // Tạo một hàng đợi mới
//        MyLinkedListQueue queue = new MyLinkedListQueue();
//
//
//        // Thêm các phần tử vào hàng đợi
//        queue.enqueue(10); // Thêm phần tử 10
//        queue.enqueue(20); // Thêm phần tử 20
//        queue.enqueue(30); // Thêm phần tử 30
//
//        // Lấy và xóa các phần tử khỏi hàng đợi, in ra giá trị của chúng
//        System.out.println("Dequeued: " + queue.dequeue().key); // Lấy và in phần tử 10
//        System.out.println("Dequeued: " + queue.dequeue().key); // Lấy và in phần tử 20
//        System.out.println("Dequeued: " + queue.dequeue().key); // Lấy và in phần tử 30
//
//        // Thử lấy phần tử khi hàng đợi rỗng, kết quả sẽ là null
//        System.out.println("Dequeued: " + queue.dequeue()); // In ra null (hàng đợi rỗng)
//        TreeMap<String, String> treeMap = new TreeMap<>();
//        Stack<String> stack = new Stack<>();
//        stack.push("Hello");
//        stack.push("World");
//        for (String s : stack) {
//            System.out.println(s);
//        }
        System.out.println("username".hashCode());
    }
}